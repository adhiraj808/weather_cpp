# Audio Assets

Place optional weather sound files here.

Recommended filenames:

- `sunny.wav`
- `rainy.wav`
- `snow.wav`
- `thunderstorm.wav`
- `cloudy.wav`

The app also supports legacy files from the `sounds` folder and will look for common aliases such as `clear`, `rain`, and `storm`.
